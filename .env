# PostgreSQL for Order Service
POSTGRES_DB=order_db
POSTGRES_USER=order_user
POSTGRES_PASSWORD=order_password
POSTGRES_PORT=5432

# MongoDB for Inventory Service
MONGO_INITDB_DATABASE=inventory_db
MONGO_INITDB_ROOT_USERNAME=inventory_user
MONGO_INITDB_ROOT_PASSWORD=inventory_password
MONGO_AUTH_DB=admin
MONGO_PORT=27017

# Database connection strings
MONGO_URI=mongodb://inventory_user:inventory_password@localhost:27017/inventory_db?authSource=admin
DATABASE_URL=postgres://order_user:order_password@localhost:5432/order_db?sslmode=disable

# Service addresses
INVENTORY_ADDR=localhost:50051
PAYMENT_ADDR=localhost:50052

# Database connections (fixed variable names)
DB_URI=postgres://order_user:order_password@localhost:5432/order_db?sslmode=disable
MIGRATIONS_DIR=migrations
